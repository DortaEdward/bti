const stringConfig = {
  type:String,
  required:true,
}
const numberConfig = {
  type:Number,
  required:true,
}

const booleanConfig = {
  type:Boolean,
  required:true
}

module.exports = {
  stringConfig,
  booleanConfig,
  numberConfig
}